# Radar V1 — presupuesto reconciliado y validación única

EXECUTION PREFLIGHT / DELIVERY DESIGN
- Rol: Dirección de Ejecución en Codex desktop, checkout existente; no se infiere trust por documentación. Resultado aprobado en archivo del usuario f8aaac78: nuevo presupuesto de costo USD5, después una sola corrida viva; publicación sólo de pieza elegible revisada. No rediseño ni reapertura de recuperación Fase A.
- Canon leído: Alanos98306d33381e94003948b343a53ea8cdfa740291; Execution_Runtime_Contract, Decision Framework, Capacity Policy, Content Engine y Recuperación. Portal HEAD d916220ee72224295ac3937765ceb5dabf5885e2; issue56 y receiptPR83 leídos. Cambios concurrentes verificados, sin colisión.
- Tamaño L / T4 / R3 por contabilidad y RPC productivos. Un frente de datos y uno de integración, revisión independiente antes de promoción. Centralizar checks finales, no repetir Fase A ni crear runtime nuevo.
- A1: extender la persistencia privada existente con cuenta de costo/holds y entradas por corrida auditables. El ledger viejo6/USD9 y api_usage históricos permanecen intactos. Migración valida correspondencia con las seis reservas y reconcilia evidencia guardada; incertidumbre mantiene hold, nunca se convierte en costo o cero inventado.
- Techo operativo USD5; hold existenteUSD1.50 por corrida; costo reconciliado conservador desde tokens/web-search registrados, no factura. Reserva atómica bajo lock: costo + holds + nuevo hold <= techo. Reconciliación idempotente conserva reserva original, costo, liberación y evidencia. Errores previos a autorización de proveedor cierran a cero; llamadas emitidas sin telemetría mantienen hold. Límites operativos separados del costo: configuración de frecuencia existente y una única corrida nueva autorizada por este mandato.
- Integración mantiene endpoints y n8n; no nueva llamada hasta A mergeada/desplegada/verificada. Telemetría tardía de una corrida vencida puede reconciliar sin reabrir su estado editorial. Callback repetido no duplica gasto. No borrar historial ni liberar incertidumbre por timeout solo.
- Aceptación: migración/SQL, dos sesiones concurrentes de PostgreSQL real, además de fixtures PGlite, fixtures de reserva/finalización/fallos/timeout/cancelación/duplicados, RLS/roles/tenant, admisión/UI de costo/holds, tests/lint/typecheck/build y CI. UI sólo para prueba efectiva de flujo; no nuevas pantallas.
- B: elegir estrategia contra corpus actual; una sola corrida desde Portal. NO_PUBLICATION/REJECT/FAILED/timeout/defecto técnico => STOP y diagnóstico, ninguna segunda corrida. Scheduler/autopublicación OFF. El presupuesto USD5 no autoriza loops. Revisión editorial/publicación conservan gates; pedir sólo una aprobación editorial concreta si la pieza lo requiere.
- Producción autorizada después de checks. Rollback: deshabilitar nuevas admisiones y volver a versión anterior sin borrar cuenta/entradas/evidencia; ledger viejo congelado protege fail-closed. No rotación de credenciales ni cambios de otros clientes.
- BUDGET_RISK: detener expansión si conciliación no puede probarse, existe gasto que excede USD5 o hace falta otra arquitectura/llamada. La aceptación positiva no se fabrica.


## Implementación y evidencia de A (antes de promoción)

- Nuevo ledger privado conserva las seis reservas originales y deriva costos con precios técnicos existentes de gpt-5-mini. Cuenta USD5, hold USD1.50, una autorización nueva y frecuencia separada; no actualización/refund del ledger legado.
- Backend guarda respuestas sanitizadas y telemetría antes de replay/portada; respuestas tardías pueden reconciliar costos sin reabrir una corrida cancelada/vencida. Evidencia incompleta mantiene hold residual. La UI distingue costo, hold, disponible e historial.
- Revisión independiente de código/SQL: sin P1/P2; reserva y reconciliación usan el mismo lock, costo monotónico y permisos exclusivos del servicio. Validación final de rollout depende de CI.
- Pruebas focalizadas de admisión/callback/telemetría/UI y typecheck/lint pasaron. El primer global detectó incompatibilidad de la migración con instalación vacía: corregida admitiendo sólo historial consistente 0/0 o 6/9. El test de fuentes excedió su timeout bajo carga paralela; ejecución focalizada posterior PASS sin cambiar test ni renderizador.
- Build local limitado por el sandbox al abrir un puerto interno de Turbopack, incluso con escalación; no es evidencia de build exitoso. Se requiere el build de CI antes de merge/deploy.
- Supabase advisor anterior: sólo avisos existentes (RLS privado sin policies y RPC autenticadas con autorización interna). Las nuevas tablas financieras no reciben permisos API directos.
- Estrategia B preparada contra el corpus de 18 notas: investigar una actualización oficial reciente de automatización de Microsoft, usando «Agregar fuente» existente. Evitar Meta Business Agent, fuentes ya publicadas y métricas/testimonios comerciales. Verificar fecha/disponibilidad y consecuencias operativas; QA y gates conservan autoridad para rechazar. No se ejecutó todavía ninguna llamada nueva al proveedor.
- Integración local: 408/408 tests en 66 suites PASS con dos workers. Export n8n intacto/verificado. SQL PGlite PASS y PostgreSQL17 de dos sesiones PASS: segunda espera Lock, una entrada nueva, newRuns1, costo0.112158, hold1.50 y legado9; base de prueba eliminada. Revisión independiente R3 final PASS, sin P1/P2. CI incluye esa prueba de concurrencia con servicio aislado, sin proveedor.
