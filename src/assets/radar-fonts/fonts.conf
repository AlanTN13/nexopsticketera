<?xml version="1.0"?>
<!DOCTYPE fontconfig SYSTEM "urn:fontconfig:fonts.dtd">
<fontconfig>
  <!-- Only explicit fontfile inputs; no host font-directory dependency. -->
  <cachedir>/tmp/nexops-radar-fonts</cachedir>
</fontconfig>
