# NexOps — Gobierno de esta ejecución

Las instrucciones superiores de la plataforma y los controles reales de acceso prevalecen siempre. Las reglas del framework de abajo complementan el gobierno; no lo reemplazan.

Antes de trabajo sustantivo, leer por una vía autorizada la versión vigente de `Alan/01_Architecture/Execution_Runtime_Contract.md` en `AlanTN13/Alanos` y registrar la revisión consultada. URL canónica: https://github.com/AlanTN13/Alanos/blob/main/Alan/01_Architecture/Execution_Runtime_Contract.md

Una referencia no es una lectura ni un import automático. Si el contrato no está disponible, detener cambios y declarar BLOCKED_EXTERNAL; no reconstruirlo de memoria ni explorar otras superficies por reflejo. Una sesión ya abierta no se da por actualizada.

Contexto del producto: `Alan/02_ProyectosPropios/NexOps/Pendientes/Tiketera_NexOps.md` en Alanos. Verificar decisiones vigentes y la superficie técnica relevante; no leer todo AlanOS. Para Dirección de Ejecución sigue aplicando el proyecto trusted Alanos del piloto; este puente no certifica trust ni configura sandbox.

Dejar EXECUTION PREFLIGHT: rol/superficie real, resultado autorizado, contexto/revisión, budget y riesgo, dentro/fuera de alcance, aceptación, permisos/recuperación y STOP/BUDGET_RISK. Una consulta o asumir rol no autoriza implementar. Dirección Estratégica puede administrar documentación aprobada vía Git sin fingir una sesión Codex local.

No ampliar alcance por hallazgos no bloqueantes. Si un defecto invalida aceptación, exactitud de métricas, aislamiento o seguridad, no declarar éxito: resolver dentro del contrato o escalar la desviación material. Validar proporcionalmente sin omitir controles obligatorios; no duplicar navegación, checks o despliegues. No eludir una denegación de acceso.

Cerrar con evidencia durable y Knowledge Delta; separar merge, producción y acceso real del cliente. No tocar producción, secretos, datos reales, permisos masivos ni publicaciones fuera de la autorización. El contrato documental no es un bloqueo técnico de cuota ni una prueba de obediencia.

<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->
